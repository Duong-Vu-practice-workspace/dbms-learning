# 4.3.1.2: tạo hàm trả về kiểu dữ liệu bảng
- hàm nội tuyến trả về dữ liệu kiểu bảng
```sql
SELECT * FROM dbo.
```
- hàm chứa nhiều lệnh trả về dữ liệu kiểu bangr(multi-statement table-valued function)
```sql
SELECT * FROM dbo.func_TONGSV(1)
```
# Thay đổi hàm
- ALTER FUNCTION
